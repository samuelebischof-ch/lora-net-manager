import { SensornamePipe } from './sensorname.pipe';

describe('SensornamePipe', () => {
  it('create an instance', () => {
    const pipe = new SensornamePipe();
    expect(pipe).toBeTruthy();
  });
});
